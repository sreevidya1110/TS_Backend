Flask==2.0.2
bcrypt==3.2.0
pymongo==3.12.0
transformers==4.13.0
accelerate==0.6.0
python-docx==0.8.11
docx==0.2.4
# Required dependencies

# Build dependencies
libffi-dev; sys_platform == 'linux'
gcc; sys_platform == 'linux'
make; sys_platform == 'linux'

# Additional dependencies
# Add any other dependencies your project requires here
